var altura = window.screen.availHeight
var largura = window.screen.availWidth

// document.write('Altura do navegador é: ' + altura + '<br/>')
// document.write('Altura do navegador é: ' + largura)

if(largura < 500) {
    document.write('Lógica para impressão do menu versão mobile')
} else {
    document.write('Lógica para impressão do menu versão web')
}

// Obter a largura da tela em pixels
    // var larguraTela = window.screen.width;

// Obter a altura da tela em pixels
    // var alturaTela = window.screen.height;

// Obter a largura disponível da tela em pixels (descontando barras de ferramentas, etc.)
    // var larguraDisponivel = window.screen.availWidth;

// Obter a altura disponível da tela em pixels (descontando barras de ferramentas, etc.)
    // var alturaDisponivel = window.screen.availHeight;

// Imprimir as informações da tela no console
    // console.log("Largura da tela: " + larguraTela);
    // console.log("Altura da tela: " + alturaTela);
    // console.log("Largura disponível da tela: " + larguraDisponivel);
    // console.log("Altura disponível da tela: " + alturaDisponivel);
