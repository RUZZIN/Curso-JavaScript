window.location.href="http://wwww.google.com"

// Obter a URL completa da página
    // var urlCompleta = window.location.href;

// Obter o hostname do servidor da URL
    // var hostname = window.location.hostname;

// Obter o caminho (pathname) da URL
    // var caminho = window.location.pathname;

// Obter o protocolo da URL (http:, https:, file:, etc.)
    // var protocolo = window.location.protocol;

// Redirecionar para uma nova página
    // window.location.href = "https://www.example.com";

// Recarregar a página atual
    // window.location.reload();
